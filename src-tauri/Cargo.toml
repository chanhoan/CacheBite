[package]
name = "cachebite"
version = "0.1.0"
description = "A local desktop pet for Claude and Codex usage"
authors = ["CacheBite contributors"]
edition = "2021"
rust-version = "1.77.2"

[features]
default = ["tauri-ipc"]
tauri-ipc = []
webdriver = ["dep:tauri-plugin-wdio-webdriver"]

[lib]
name = "cachebite_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.4.1", features = [] }

[dependencies]
reqwest = { version = "0.13.4", default-features = false, features = ["json", "rustls"] }
secrecy = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2.8.5", features = ["protocol-asset"] }
tauri-plugin-notification = "2.3.3"
tauri-plugin-autostart = "2.5.1"
tauri-plugin-wdio-webdriver = { version = "1.2.0", optional = true }
thiserror = "2"
time = { version = "0.3", features = ["formatting", "parsing", "serde", "serde-well-known"] }
tokio = { version = "1", features = ["io-util", "macros", "process", "rt", "sync", "time", "test-util"] }
tracing = "0.1"
zeroize = "1"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = ["Win32_Storage_FileSystem", "Win32_System_SystemInformation"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
